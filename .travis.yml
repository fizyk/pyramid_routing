language: python
python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"
install:
    - "python setup.py install"
    - "pip install tzf.pyramid_routing[tests] coveralls --use-mirrors"
script:
    - coverage run --source=tzf.pyramid_routing setup.py nosetests
after_success:
    - coveralls
